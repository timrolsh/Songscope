const db = require("./db_connect");

function getUserInfo() {}

// get the average rating, comments, and replies for a work on spotify (music)
function getWorkInfo() {}



module.exports = {
    getUserInfo: getUserInfo,
    getWorkInfo: getWorkInfo
};
